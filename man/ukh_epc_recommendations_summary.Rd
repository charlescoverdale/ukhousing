% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epc.R
\name{ukh_epc_recommendations_summary}
\alias{ukh_epc_recommendations_summary}
\title{Summarise EPC improvement recommendations for a local authority}
\usage{
ukh_epc_recommendations_summary(
  la,
  type = c("domestic", "non-domestic", "display"),
  refresh = FALSE
)
}
\arguments{
\item{la}{Character. Local authority GSS code.}

\item{type}{Character. \code{"domestic"} (default), \code{"non-domestic"}, or
\code{"display"}.}

\item{refresh}{Logical. Re-download bulk ZIP? Default \code{FALSE}.}
}
\value{
A data frame with columns \code{improvement_id},
\code{improvement_summary}, \code{count}, \code{mean_indicative_cost} (where
numeric costs are reported), ordered by \code{count} descending.
}
\description{
Aggregates the improvement recommendations across certificates in a
local authority, returning the frequency of each recommendation and
the mean estimated cost and savings where available.
}
\details{
This uses the bulk per-LA ZIP download rather than the paginated
API, which is much faster for this aggregation. Requires EPC API
credentials.
}
\examples{
\dontrun{
recs <- ukh_epc_recommendations_summary("E09000033")
head(recs)
}
}
\seealso{
Other energy performance certificates: 
\code{\link{ukh_epc_bulk}()},
\code{\link{ukh_epc_certificate}()},
\code{\link{ukh_epc_search}()},
\code{\link{ukh_epc_summary}()}
}
\concept{energy performance certificates}

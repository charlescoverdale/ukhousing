% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppd.R
\name{ukh_ppd}
\alias{ukh_ppd}
\title{Price Paid Data}
\usage{
ukh_ppd(
  year = as.integer(format(Sys.Date(), "\%Y")),
  la = NULL,
  postcode = NULL,
  property_type = NULL,
  new_build = NULL,
  tenure = NULL,
  from = NULL,
  to = NULL,
  refresh = FALSE
)
}
\arguments{
\item{year}{Integer. Year of transactions to fetch. Defaults to the
current calendar year.}

\item{la}{Optional character. Local authority name (matched
case-insensitively against the \code{district} field).}

\item{postcode}{Optional character. Postcode or postcode prefix.}

\item{property_type}{Optional character. One of \code{"detached"},
\code{"semi"}, \code{"terraced"}, \code{"flat"}, or \code{"other"}. Also accepts the
single-letter codes \code{"D"}, \code{"S"}, \code{"T"}, \code{"F"}, \code{"O"}.}

\item{new_build}{Optional logical. If \code{TRUE}, only newly built
properties. If \code{FALSE}, only established properties.}

\item{tenure}{Optional character. \code{"freehold"} or \code{"leasehold"}.}

\item{from, to}{Optional character or Date. Date range within the
year (YYYY-MM-DD).}

\item{refresh}{Logical. Re-download the yearly file even if cached?
Default \code{FALSE}.}
}
\value{
A data frame of individual transactions with columns
\code{transaction_id}, \code{price}, \code{date}, \code{postcode}, \code{property_type},
\code{new_build}, \code{tenure}, \code{paon}, \code{saon}, \code{street}, \code{locality},
\code{town}, \code{district}, \code{county}, \code{category}, \code{record_status}.
}
\description{
Fetches individual property transaction records from the HM Land
Registry Price Paid Data, filtered by local authority, postcode,
property type, and other criteria.
}
\details{
The full yearly CSV is ~150 MB (about 900,000 transactions). This
function downloads the yearly file, caches it, and then filters in
memory. Memory footprint during the call is roughly 1 GB because R
data frames are considerably larger than the source CSV; on
memory-constrained machines, prefer \code{\link[=ukh_ppd_address]{ukh_ppd_address()}} for
postcode lookups or \code{\link[=ukh_ppd_summary]{ukh_ppd_summary()}} for aggregated stats.
Subsequent queries against the same year use the cache. For
multi-year queries, call the function once per year or use
\code{\link[=ukh_ppd_years]{ukh_ppd_years()}}.
}
\examples{
\donttest{
op <- options(ukhousing.cache_dir = tempdir())

# Westminster flats sold in 2024
wm <- ukh_ppd(2024, la = "Westminster", property_type = "flat")
head(wm)

options(op)
}
}
\seealso{
Other price paid data: 
\code{\link{ukh_ppd_address}()},
\code{\link{ukh_ppd_bulk}()},
\code{\link{ukh_ppd_summary}()},
\code{\link{ukh_ppd_transaction}()},
\code{\link{ukh_ppd_years}()}
}
\concept{price paid data}

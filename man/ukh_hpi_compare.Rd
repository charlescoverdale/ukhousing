% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpi.R
\name{ukh_hpi_compare}
\alias{ukh_hpi_compare}
\title{Compare UK HPI across multiple regions}
\usage{
ukh_hpi_compare(
  regions,
  measure = "avg_price",
  from = NULL,
  to = NULL,
  refresh = FALSE
)
}
\arguments{
\item{regions}{Character vector of region slugs, GSS codes, or names.}

\item{measure}{Character. Which measure to return. One of
\code{"avg_price"}, \code{"hpi"}, \code{"pct_change_annual"}, \code{"pct_change_monthly"},
or \code{"sales_volume"}. Default \code{"avg_price"}.}

\item{from, to}{Optional character or Date. Date range.}

\item{refresh}{Logical. Re-download even if cached? Default \code{FALSE}.}
}
\value{
A wide data frame with a \code{date} column and one column per
region.
}
\description{
Fetches the UK House Price Index for several regions and returns a
wide data frame with one measure (e.g. average price or annual \%
change) as a column per region. Useful for regional comparison charts.
}
\examples{
\donttest{
op <- options(ukhousing.cache_dir = tempdir())

prices <- ukh_hpi_compare(
  c("london", "manchester", "newcastle-upon-tyne"),
  measure = "avg_price",
  from = "2015-01-01"
)
head(prices)

options(op)
}
}
\seealso{
Other house price index: 
\code{\link{ukh_hpi}()},
\code{\link{ukh_transactions}()}
}
\concept{house price index}

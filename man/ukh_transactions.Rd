% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpi.R
\name{ukh_transactions}
\alias{ukh_transactions}
\title{Transaction volumes for a region}
\usage{
ukh_transactions(region, from = NULL, to = NULL, refresh = FALSE)
}
\arguments{
\item{region}{Character. Region slug, GSS code, or common name.}

\item{from, to}{Optional. Date range (YYYY-MM-DD).}

\item{refresh}{Logical. Re-download? Default \code{FALSE}.}
}
\value{
A data frame with \code{date}, \code{region}, and \code{sales_volume}.
}
\description{
Returns monthly residential transaction counts for a UK region.
This is a thin wrapper over \code{\link[=ukh_hpi]{ukh_hpi()}} that extracts the
\code{sales_volume} column with its date. Transaction volumes lag the
headline index by approximately five months because the Land
Registry needs the full transaction set to settle.
}
\examples{
\donttest{
op <- options(ukhousing.cache_dir = tempdir())
tx <- ukh_transactions("england", from = "2020-01-01")
head(tx)
options(op)
}
}
\seealso{
Other house price index: 
\code{\link{ukh_hpi}()},
\code{\link{ukh_hpi_compare}()}
}
\concept{house price index}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epc.R
\name{ukh_epc_search}
\alias{ukh_epc_search}
\title{Search domestic Energy Performance Certificates}
\usage{
ukh_epc_search(
  postcode = NULL,
  la = NULL,
  property_type = NULL,
  rating = NULL,
  built_form = NULL,
  from = NULL,
  to = NULL,
  size = 1000L,
  max_records = 10000L,
  type = c("domestic", "non-domestic", "display")
)
}
\arguments{
\item{postcode}{Optional character. Postcode or partial postcode.}

\item{la}{Optional character. Local authority GSS code (e.g.
\code{"E09000033"} for Westminster).}

\item{property_type}{Optional character. \code{"House"}, \code{"Flat"},
\code{"Bungalow"}, \code{"Maisonette"}, \code{"Park home"}.}

\item{rating}{Optional character. Current energy rating (\code{"A"} to
\code{"G"}).}

\item{built_form}{Optional character. \code{"Detached"}, \code{"Semi-Detached"},
\code{"End-Terrace"}, \code{"Mid-Terrace"}, \code{"Enclosed End-Terrace"},
\code{"Enclosed Mid-Terrace"}.}

\item{from, to}{Optional. Lodgement date range (YYYY-MM-DD).}

\item{size}{Integer. Results per page, max 5000. Default 1000.}

\item{max_records}{Integer. Maximum total records to fetch across
pages. Default 10000. Set higher for bulk analysis.}

\item{type}{Character. Register to query: \code{"domestic"} (default,
housing), \code{"non-domestic"} (commercial buildings), or \code{"display"}
(DECs for public buildings).}
}
\value{
A data frame of certificates. Columns include \code{lmk_key},
\code{address}, \code{postcode}, \code{uprn}, \code{local_authority}, \code{constituency},
\code{property_type}, \code{built_form}, \code{inspection_date}, \code{lodgement_date},
\code{current_energy_rating}, \code{current_energy_efficiency},
\code{potential_energy_rating}, \code{potential_energy_efficiency},
\code{total_floor_area}, \code{co2_emissions_current}, and more.
}
\description{
Queries the MHCLG EPC Open Data service for domestic certificates
matching the given filters. Results are paginated automatically
using \code{search-after} tokens (not the \code{from} parameter, which caps
at 10,000 records).
}
\details{
Registration at \url{https://epc.opendatacommunities.org/} is
required and free.
}
\examples{
\dontrun{
# Requires EPC API credentials
certs <- ukh_epc_search(postcode = "SW1A 1AA")
head(certs)

# All E-rated flats in Westminster lodged since 2020
wm <- ukh_epc_search(
  la = "E09000033", property_type = "Flat",
  rating = "E", from = "2020-01-01"
)
}
}
\seealso{
Other energy performance certificates: 
\code{\link{ukh_epc_bulk}()},
\code{\link{ukh_epc_certificate}()},
\code{\link{ukh_epc_recommendations_summary}()},
\code{\link{ukh_epc_summary}()}
}
\concept{energy performance certificates}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/planning.R
\name{ukh_planning}
\alias{ukh_planning}
\title{Query planning.data.gov.uk}
\usage{
ukh_planning(
  dataset,
  la = NULL,
  limit = 1000L,
  format = c("data.frame", "raw")
)
}
\arguments{
\item{dataset}{Character. Dataset slug. Common values:
\code{"planning-application"}, \code{"brownfield-land"}, \code{"local-plan"},
\code{"conservation-area"}, \code{"listed-building"},
\code{"article-4-direction-area"}, \code{"tree-preservation-zone"},
\code{"flood-risk-zone"}. See \code{\link[=ukh_planning_datasets]{ukh_planning_datasets()}} for the full
list.}

\item{la}{Optional character. Local authority name (matched
case-insensitively against the \code{organisation} field).}

\item{limit}{Integer. Maximum records to return. Default \code{1000}.}

\item{format}{Character. Response format. \code{"data.frame"} (default)
returns tidy data. \code{"raw"} returns the parsed JSON list.}
}
\value{
A data frame (or list if \code{format = "raw"}).
}
\description{
Fetches records from the Digital Land planning data platform,
which hosts over 100 datasets on planning applications, brownfield
land, local plans, conservation areas, listed buildings, flood risk
zones, and more.
}
\examples{
\donttest{
op <- options(ukhousing.cache_dir = tempdir())
bf <- ukh_planning("brownfield-land", limit = 100)
head(bf)
options(op)
}
}
\seealso{
Other planning: 
\code{\link{ukh_planning_datasets}()}
}
\concept{planning}
